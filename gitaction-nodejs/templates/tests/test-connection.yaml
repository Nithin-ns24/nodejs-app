apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gitaction-nodejs.fullname" . }}-test-connection"
  labels:
    {{- include "gitaction-nodejs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gitaction-nodejs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
