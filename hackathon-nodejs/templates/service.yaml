apiVersion: v1
kind: Service
metadata:
  name: {{ include "hackathon-nodejs.fullname" . }}
  labels:
    {{- include "hackathon-nodejs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hackathon-nodejs.selectorLabels" . | nindent 4 }}
